# Generated by build.rs on each build; not committed. cargo-deb picks them up
# from here at package time (build runs before packaging).
cfcli.bash
_cfcli
cfcli.fish
_cfcli.ps1

# Keep the hand-written dynamic-completion glue.
!addendum.bash
!addendum.zsh
!addendum.ps1
